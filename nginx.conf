server {
    listen 3000 default_server;
    listen [::]:3000 default_server;

    root /home/ktlyn/app;

    index index.html index.htm

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}
